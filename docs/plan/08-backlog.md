# 08. バックログ（不具合・技術的負債・改善）

ID は種別＋連番。優先: P1（次スプリント）/ P2（次マイルストーン）/ P3（余裕があれば）。

## 不具合（BUG）

| ID | 内容 | 再現 | 優先 | 対応 |
|----|------|------|------|------|
| BUG-001 | 復活直後、開始地点近くのゾンビ湧きで連続死しやすい | 1 面開始地点で立ち止まる | 済 | 開始地点・中間地点 ±96 は湧き禁止、復活後 2 秒は敵弾なし（`src/balance.js`、`test/balance.test.js`、commit 94f5396） |
| BUG-002 | 敵弾の当たり判定が生成スプライトより小さい／ずれる | 毒玉の見た目と接触位置 | 済 | `SHOT_HIT_RATIO` 0.6 × 生成寸法（例: 毒 8×12、血 5×10 世界単位）。`test/balance.test.js`、commit 0d46a4e |
| BUG-003 | 生成台帳が並列実行で計数競合 | 複数プロセス同時実行 | 済 | `tools/gemini_gen.py record()` が `gen_ledger.lock` を `fcntl.flock` し、一時ファイル経由で書く。commit 0d46a4e |
| BUG-004 | キノコ妖精 2 コマ目が 1 コマ目と形が大きく違い瞬きが荒い | カタログ敵シート | 済 | 再生成 v2（羽の上下・腕・体高 3 セル差を明記）。83×94 / 84×113、15 色。commit 0d46a4e |
| BUG-005 | 城の「空」が一枚絵（アーチ窓）で遠景と密度が合わない | 3 面 | 済 | 奥壁として再生成（台帳 81）、2 px/セル横タイル（`world.js SKY_PX`） |
| BUG-006 | 私服の袖口・襟の色分けが単純（規則置換の限界） | カタログ主人公シート | P3 | 手修正 or 置換規則の細分化 |
| BUG-007 | hurt2（白飛び）コマが未使用 | — | 済（2026-09-11、Sprint Q） | 被弾直後 0.1 秒（`HURT_FLASH_T`）は hurt2、以後 0.25 秒は hurt（`player.js frame()`、衣装に hurt2 が無ければ hurt）。`test/player-frames.test.js`、`test-results/shots/hurt2_frame.png`。golden の画素ハッシュ（90・600 フレーム目）は不変 |
| BUG-008 | 中景 B/C が主中景と高さが違う場合に連結から除外されるため、城は 1 種のみ | 3 面 | 済（2026-09-12、2 リクエスト、台帳 235〜236/280） | 城用に壁面 2 種を生成。v1 の B/C は小物の列（高さ 127 / 74。74 は主中景 164 の 50% 未満で除外、127 は壁の隣に窓が浮いて連結）。v2 は主中景 v1 raw を参照して同じ壁面の帯として配置 B（薔薇窓・扉）/ C（窓 3・鎖・バルコニー）を生成: **268×162 / 263×169**（主中景 267×164、いずれも 32 色）。3 本とも連結対象になり城の中景は 3 種（合計幅 798）。golden の stage3 ハッシュ（90・600 フレーム）を更新（軌跡は不変）。証跡 `test-results/shots/cmp_stage3_20.png`・`_50.png`・`_80.png` |
| BUG-014 | `npm run build` の dist に生成素材 PNG が 1 枚も入らず、preview で全素材がフォールバック表示になる（`loader.js` が `import.meta.url` 基準で `../../assets/…` を解決し、dist/assets/main-*.js からはサイトの外を指す） | `npm run build && npm run preview` | 済 | ページ URL（`document.baseURI`）基準に変更、`vite.config.js copySprites` が `assets/sprites` を dist にコピー。`tools/check_dist.mjs` で 235 読込を確認（Sprint J） |
| BUG-015 | スマホ縦（幅 390）でキャンバスが 768 px のまま横にはみ出す（`fit()` の最小倍率が 1） | iPhone 13 エミュレーション | 済 | 整数倍で入らないときは幅に合わせて縮小。`e2e/mobile.spec.js`（Sprint I） |

## 技術的負債（DEBT）

| ID | 内容 | 優先 | 対応 |
|----|------|------|------|
| DEBT-001 | 敵・粒子が `Math.random` 依存（デモ再生・E2E の決定論性なし） | 済（敵・ボス・アイテム・弾は `grand()`。粒子・画面揺れは演出のため対象外） | 世界シード RNG へ置換 |
| DEBT-002 | E2E がブラウザ手動スクリプト | 済（`npm run e2e`、Playwright/Chromium headless。CI 定義はリモート未設定のため未着手） | Playwright 化、CI |
| DEBT-003 | 文字列ドット絵（旧 `src/gfx/sprites/*`）が残存（炎・フォールバック） | 済（2026-09-13、Sprint S、0 リクエスト） | 炎（shots/fire1・fire2）は既に生成素材だった。文字列定義 38 種は全部 manifest に生成素材があり、装飾 9 種は全 8 面で描かれないことを計測して `src/gfx/sprites/*`・`tools/gen_player.py`・`makeSprite/buildSheet`・`COSTUMES` を削除。読めない素材は manifest 寸法の市松プレースホルダ（ADR-0040、`test/loader.test.js` 4 件、`e2e/boot.spec.js`）。調査で BUG-023・BUG-024 を発見 |
| DEBT-004 | `world.js` が描画・当たり判定・ボス管理を抱えて肥大 | 済（Sprint P、2026-09-11） | `stage/render.js`（描画 6 関数）/ `stage/collision.js` に分割。world.js 395 → 202 行。ボス進行は残（`docs/architecture.md` §5） |
| DEBT-005 | 背景の密度が層ごとに違う（空 3・遠 2・中 1） | 済（遠景 1 倍化、A/B 連結。空 3 は残存: 城 2） | 遠景 A/B 生成（台帳 75〜82） |
| DEBT-006 | HUD のミニフォントが手描き 3×5 | 済（DotGothic16 16px） | 8px 英数フォントに置換 |
| DEBT-007 | `specs.json` の `box` が検査専用で意味が曖昧 | 済（2026-09-11、Sprint Q） | 改名せず定義を明文化。`box` は prompt の Logical box（`gemini_gen.py`）・後処理 `--logical`（`build_sprites.py`）・manifest `fits` の QA の 3 箇所が共有する「論理箱」で検査専用ではないため `max` は不適。`docs/gen-pipeline.md` §1 に行を追加、スキル SKILL.md の説明を修正 |
| DEBT-008 | 生成スクリプトが参照プロジェクトの `.venv` と `.env` に依存 | 済（Sprint K） | 本リポに `requirements.txt`（fonttools/pillow/numpy/google-genai）と `.env.example` を置き、`python3 -m venv .venv` で完結。`gemini_gen.py` は 環境変数 → 本リポ `.env` → 参照プロジェクト `.env` の順に探す |

## 改善（IMP）

| ID | 内容 | 優先 |
|----|------|------|
| IMP-001 | ヒットストップ・撃破フラッシュ・アイリスワイプ | 済（`src/fx.js`、F1/F2/F4、commit 0d46a4e） |
| IMP-002 | ボス登場演出（名前テロップ・入場モーション 2 コマ） | 済（レターボックス＋名前バナー 2 秒、攻撃コマ doll2/teddy2/noir2。F6） |
| IMP-003 | ゲームパッド・キーコンフィグ | 済（Gamepad API standard mapping、オプション画面で 7 操作を再割り当て。commit e502530） |
| IMP-004 | セーブ・オプション・ステージセレクト | 済（`lyrica_save`: 進行・音量・ミュート・キー・ハイスコア。「つづきから」は最大到達章。ステージセレクトは未実装） |
| IMP-005 | デモ（アトラクト）モード | 済（記録はボット。人手収録で差し替え可） |
| IMP-006 | 溜め魔法 4 種 | 済（`src/entities/magic.js`） |
| IMP-007 | 死亡地点ログの収集と可視化（カタログ「ステージ構成」に重ねる） | 済（`src/deathlog.js`: localStorage `lyrica_deaths` に座標・原因・経過秒・周回を 600 件まで。カタログ「ステージ構成」に × を重ね、面ごとに件数・原因・64 単位の多発地点を表示。`tools/gather_deaths.mjs` が無敵なし・残機無限のボットで収集し `docs/plan/logs/deaths-<日付>.json/.md` に書き出す。初回 2026-09-09: 2 周 × 8 面で 118 件） |
| IMP-008 | 英語 UI | 済（Sprint I: `src/i18n.js`、`story.js` 両言語、設定 `lang`、オプション行、`index.html` 説明文。未翻訳: `projectiles.js` / `magic.js` の武器・魔法名〔カタログ専用〕、`catalog.html`） |
| IMP-009 | マイルド表現オプション（血の色） | P3 |
| IMP-010 | プレス機のブロック絵が 47×56 セル（≈1 タイル）で装飾に埋もれる。単体 1 リクエストで 3 タイル級に描き直す | 済（`gimmick-press` spec、PART SIZES 指定で 157×147、台帳 178。`gimmick-workshop` は `skip_out` で press を出さない） |
| IMP-011 | 工房の遠景 B（237×59）が A（248×140）と高さ不一致で不使用。参照付きで再生成 | 済（2 回目で 247×116、台帳 179〜180。1 回目は PART SIZES を文章で書いても 68 高。箱を 260×150 に絞り「窓 140 セル・箱の上端まで」と部位で書いて通った） |
| IMP-012 | 遊園地の中景 A に暗い地面板（キー残り）が残る。STRICT KEY を強めて再生成、または後処理で下端の暗帯を落とす | 済（中景 6 枚は BUG-011 で再生成。2026-09-09 に全 41 層の下 6 行を機械計測: 不透明率 0.85 超は遠景 12 枚と castle_mid・workshop_mid で、目視ではいずれも地平線の地面帯か壁面で板ではない。tower_far/far2・workshop_far の床帯 8〜10 行は残置） |
| IMP-013 | 第六章の強制スクロール区間は「走る車 `R` に乗る」代替。本来の強制スクロール（カメラ固定速度・画面外で死亡）は縦スクロール実装と合わせて設計 | P2 |
| IMP-014 | 章の並び替え: 城を第三章へ戻し新ボス「砂糖の女王」を置く、ノワールを最終章（第八章）へ | 済（commit 予定） |
| IMP-015 | デモ入力ログが 4 面分のみ（第四・五章は未収録） | 済（2026-09-11、Sprint Q）: `tools/record_demos.mjs` が無敵なし・残機 2 のボット（16 変種）で全 8 面を収録し、`assets/demo/<面名>.json` に書く。再生と同じ条件（面の seed・lives 2・loop 0）で録り、ページ再読込後に `startDemo` の軌跡（60 フレームごと）が収録と一致することを検査（8 面一致）。あわせて `startDemo` が配列位置ではなく面名で照合（`findDemo`。IMP-014 の並び替えで stage3.json が涙の川、stage4.json が城の記録になっていた）。同日後半（ユーザー指示「デモの bot が賢くないので、アクション時に敵を避けるように」）: ボットを状態読み取り型に拡張。①敵・敵弾の先読み（敵弾は速度＋重力で 30 フレーム、突進する敵も同様。しゃがみ箱の上を通る弾は伏せて撃つ、それ以外は跳んだ先が安全なら跳ぶ）②同じ高さの敵は近ければ跳び越え、遠ければ止まって撃つ（低い敵はしゃがみ撃ち。後ろから迫る敵は振り向いて撃つ）③頭上から落ちる繭は反対へ逃げる（着地まで対象固定）④沈んだシロップの腕は立ち上がる前に通る／立っていれば撃つ⑤プレスは安全時間を待ち、真下では止まらず走り抜ける⑥**放物線の先読み**（プレイヤー物理と同じ定数で単発・二段（頂点で前／真下）・歩いて落ちる・トランポリンの跳ね返り・動く足場の未来位置を進め、沼・棘・落下なら「死」）で、安全かつ最も遠くへ進む跳び方だけ実行。壁に接して跳ぶと垂直になるので少し下がってから跳ぶ。素朴なボット 3 変種も残し、面ごとに「進んだ距離 − 250×死亡 − 400×ゲームオーバー」で採用。結果（旧→新）: stage1 1 死/2237→1 死/2237（素朴）、stage2 3 死/543→**2 死/2266**、stage3 3 死/524→**2 死/2566**、river 2 死/1290→**0 死/1200**、workshop 3 死/1106→**2 死/1983**、park 2 死/2799（素朴）、tower 2 死/1800（素朴）、stars 0 死/3758（素朴）。8 面とも再生一致。調整用に `--trace <面> [--bot '{json}'] [--step f --from s --to s --simdump s]` で 1 フレームごとの判断と先読み軌道を出せる。**追加調整**（同日、ユーザー指示「追加調整をして」）: 弾の回避を規則から「候補行動（走る／伏せる／下がる／跳ぶ／跳んで止まる／止まる）ごとに自分の箱を進め、全弾・突進敵の予測位置と重ならず着地も安全な最初の候補」に置換（跳ねる蛆弾は直近 6 フレームは足元までの帯、以降は床すれすれの箱）。頭の高さに浮く敵は跳び越えず、撃てるなら止まって撃ち、迫れば下がる。ボスは塞ぐ相手にせず撃ちながら進む。敵が前に居るときは 6 フレームごとに連射。落下物からの逃げは「ほぼ真下・1 秒以内」に限定。観覧車などの動く足場では安全な降り方が見つかった瞬間に跳ぶ。シロップの腕の「通れる」判定を残り距離ベースに。結果: 総死亡 11 → **6**（stage1 1、stage2 1、stage3 **0**/3668、river 0、workshop 1、park 1/2613、tower 2（素朴）、stars 0/3758）。stage2 は 2 死/2266 → 1 死/1247（トランポリン経路まで届かず距離は減）。続き（11:00）: 回避の予測範囲を 36 フレームに限定（遠い弾まで見ると全候補が「当たる」になり動けない）、跳ねる蛆弾は 14 フレーム以内で跳び越え、妖精の毒が残す毒溜まり（`w.pools`）を沼と同じ危険として先読み。総死亡 6（stage2 は 2 死/2266 に戻る）。調整用の `--simdump` に回避候補ごとの結果と弾の予測位置を追加  **続き（2026-09-12、死因の追跡）**: 8 面の総死亡 6 → **3**（第一章 1〔妖精の毒 x=2362〕、塔 2〔素朴なボット。蛆と鏡像〕、他 6 面は 0）。進んだ距離: 涙の川 2376 → 3897、工房 1965 → 2570、遊園地 2613 → 3780、第二章 3004 → 2841。直した判断: 敵弾は EnemyShot と同じ物理で軌道を先読み（重力・床で消える・跳ねる蛆は 0.5 倍反発）、重力で跳ぶ敵（熊）は速度に関係なく弾扱い、低い敵の跳び越え前に軌道が敵に触れないか（余白は時間で広げる）、撃って当たる高さなら待ち時間内は撃ち続け、12 px 未満なら下がる、止まって撃つ前に敵の方を向く（後ろ向きに撃ち続けていた）、後ろの敵へ振り向く 1 フレームで跳ばない、跳ねる敵には振り向かない、壁の手前で下がる先に敵が居れば下がらない、下がりながら跳ぶ回避候補（backJump）、プレスはベルトの実効速度で見積もり間に合わなければ近い側へ抜ける、横に動く足場だけを待つ（縦揺れの浮島で永久停止していた）。**採らなかった案**: 風船の亡霊専用の規則（跳ばない／待って撃つ／下がる）は 1 死 → 3 死に悪化したので外した（コメントに記録）  **続き 2（2026-09-12 夜）**: 総死亡 3 → **0**（8 面すべて、60 秒）。第一章 2356 → 3683、第二章 2841 → 3040、塔は賢いボットが 0 死 1445（素朴 1 死 1800）。直した判断: 落下物は状態（drop / swoop）と重力のある敵の速度だけで判定（ガーゴイルの残った vy で 2 秒逃げ続けた）、逃げる先に敵が居れば止まる、最も近いはしごへ、縦面では 40 px より下へ降りる着地を避ける、回避候補が全部だめなら当たるのが最も遅い候補、跳ぶ先の敵・弾も見てから跳ぶ（bestJump / 空中の二段）、跳ぶ直前の熊は跳躍の弧を箱にする、蛆を吐く直前は跳ばない、鏡像（軸の 150 px 手前で止まり撃って倒す。40 px 以内なら下がる）。採用基準の死亡の重み 250 → 400。ゲーム側: BUG-020（針の瞬間移動）・BUG-021（ケーキの崖落ち）を修正  **続き 3（2026-09-13、ユーザー指示「ドレスのアイテムは単位時間あたりの x が多少下がっても優先して取得」）**: 落ちている dress / golddress（220 px 以内・同じ高さ帯）へは後ろでも向かう。宝箱は足場の上に置かれているので（第二章 2 個目は地面より 48 px 上）、開ける価値がある箱（私服なら dress、ドレスで 3 個目なら golddress）が 140 px 以内・90 px 以内の高さなら、その足場へ跳び乗る跳び方を先読みで選んで登り、同じ高さで止まって撃つ（真下で 90 フレーム見つからなければ諦める）。価値の無い箱は開けない（武器が変わって狙いが狂う・乱数の消費で後の展開が変わる）。結果: 第一章・第二章で各 1 着取得、進み 3683 → 3613 / 3040 → 3034、全 8 面 0 死のまま。収録の要約に `dresses=` を出す |
| DEBT-009 | デザインカタログが歯抜け: 第三〜五章の敵・ボス（人魚・傘・人形・針・風船・ピエロ、大蛇・機械・観覧車の主）、ギミック小物（はしご・浮島・板・プレス・ベルト・歯車・車・ゴンドラ・軸）、溜め魔法演出、敵弾 cknife、新テーマの装飾が章に無い／固定リストで欠落。**全素材を manifest から機械的に列挙して網羅する**（ユーザー指示 2026-09-09） | 済（commit 予定: サイズ比較・ボス欄・装飾表・ギミック小物を manifest/DECO_MAP 駆動に、「全素材」章を追加、`test/catalog.test.js` 4 件で掲載漏れを検出） | 各章を manifest 駆動にし、掲載漏れをテストで検出 |
| DEBT-010 | ゾンビうさぎの湧きコマ（`zombieRise`）が旧手描きドット絵のまま（炎は生成に置換済み）。生成 1 リクエストで置換 | 済（台帳 134、57×63 セル） | `enemy-zombie-rise` spec を追加 |
| IMP-016 | 走りながら撃つ専用コマを走りモーションと同じ 4 コマ分用意する（現在は走行中の射撃で足を止めないため上半身の射撃ポーズが出ない）。生成 2 リクエスト（2 コマ×2）＋帽子合成 | 済（2 コマ運用に縮小。帽子なし `player-runshoot-a/b` v2 の接地コマ run1s/run3s（103×104 / 99×105、杖込み）を採用し帽子は合成。通過コマ（2・4）は v1/v2 とも杖が描かれず不採用。帽子あり生成 2 枚は idle 比 1.14〜1.24x で不採用（`skip`）。台帳 181〜186。`player.js frame()` は射撃中 12 tick ずつ run1s/run3s を交互、`test/player-frames.test.js`） |
| BUG-010 | 第一章: 墓石が透ける（灰緑の石面がキーで抜けた 1170 セル）、血溜まりが浮く（下端の滴 3 画素が接地面になっていた）、落とし穴の上に草花 | 済（ユーザー指摘 2026-09-09。後処理 `fill_holes` / `trim_thin_bottom` を装飾全 27 点に適用、seg2 の f を地面上へ） |
| BUG-011 | 全ステージで遠景・中景の上端が水平に切れる（初期の月除去用 `keep_bottom` が内容 bbox の下半分だけ残していた） | 済（全背景層から除去。ついで地面板が描かれていた中景 6 枚を STRICT KEY で再生成、台帳 169〜174） |
| BUG-012 | 同じキャラのスプライト内でコマごとの大きさが揃っていない箇所が多い（別リクエストで描いたコマは idle 比 ±12% を超えることがある: jump 94 / fall 107 / attack 117 / hurt 118 vs idle 111、私服 run3・run4 と dress の描き分け、敵の 2 コマ目も同様）。`build_sprites.py` の QA WARN を基準に、超えたコマを再生成または版固定で揃える。ユーザー指示 2026-09-09 | 一部済（2026-09-09）。**仕様の書き方を変更**: 高さ比ではなく部位ごとの論理 px を `specs.json part_sizes`（帽子 35 高／つば 57 幅、頭 40 高／髪幅 40、胴 20、スカート 24、脚 18、計 111、私服 103）に置き、`gemini_gen.py` が PART SIZES ブロックとして prompt に付与、`build_sprites.py` が出力コマの帽子つば幅・帽子高（±15%）・髪幅（±30%）を idle と同じ関数で測って WARN（ユーザー指示「パーツ単位で論理何ピクセルを占めるかを与える」）。直したコマ: jump（帽子あり v2 70×102、つば 52 vs 42。fall は v1 を `frame_use` で維持）、jump/fall 私服 v2（57×97 / 80×104）、hurt2（生成の「白く光る」が白い幽霊になっていたので hurt を白へ 55% 寄せた派生に置換、両衣装）、cake（両コマに描かれた枠線を除去、54×72 / 54×74、v2）。**残**: hurt の帽子つば 61（1.45x、傾いた帽子）、run1 つば 47（1.12x）、mermaid1 は水面上の半身で意図どおり、敵 2 コマ目の姿勢差（mushroom2 113 vs 94、gargoyle2 93 vs 83）は姿勢によるもので保留。fall_nohat 髪幅 43 vs 31 |
| IMP-017 | ボット死亡ログ 2026-09-09 の多発地点を人の視点で検証する: 第三章 x512（棘の谷、32 件）、涙の川 x1280（6 タイルの沼、12 件）、工房 x1088（プレス機、16 件）、遊園地 x2688（沼、18 件）、菓子の森 x2304（沼＋敵、17 件）。ボットは足場乗りと弾避けをしないため過大。人のテスト後に難易度調整（テスター繰延のため保留） | P2 |
| IMP-018 | 2 周目「真の結末」の専用挿絵（ending-7）を 1 リクエストで生成（現在は場面 5 を流用） | 済（2026-09-12、1 リクエスト、台帳 234/280）。`ending-7` は ending-5 と同じ物差し（`_illust_style`、尊重物 scene1 ＋ 立ち絵）。scene7 **256×256、32 色、flat 0.21（帯 ≤ 0.28）、dither 0.13（≥ 0.07）、輪郭 hue 302 / val 0.23（255〜335 / ≤ 0.23）**で §2.6 テストを通過。題材: 二度目の朝、帽子を胸に抱え中に光る心（「守りたい」）、背後に血の落ちた砂糖城。`game.js endingScenes` は既に scene7 優先。証跡 `test-results/shots/ending_scene7_loop2.png` |
| IMP-019 | 公開 URL の初回ロードが 6.6 秒（エッジ未キャッシュ、449 ファイル、転送 3.4 MB のうちフォント 2.07 MB）。2 回目以降は 0.6 秒 | 済（Sprint K）。フォントを使用文字だけに絞り（`tools/subset_font.py`、457 字 143 KB。カタログは全字形）、スプライト PNG をパレット化（`tools/optimize_pngs.py`、2.61 → 1.12 MB、可逆を画素比較で検査）、`public/_headers` で `/assets/*` を 1 年 immutable（PNG は `?v=<ビルド ID>` で破棄）。転送 3.4 → 1.28 MB、初回 2.07 秒・以後 0.47〜0.61 秒。アトラス化は転送量が収まったため見送り |
| IMP-020 | 済（Sprint L、2026-09-10。予算はユーザー承認 8〜10 に対し 4 リクエスト）: 専用敵 2 種（綿あめの繭 Y・シロップの腕 A）、専用ギミック 3 種（綿あめのトランポリン W・糖蜜のノズル D・飴の板 !）、菓子版の装飾 4 点（飴の墓標・キャンディケインの十字・砂糖菓子の花・溶けたケーキ）、レベル全面差し替え。**第二章「毒沼の菓子の森」の再設計**（ユーザー指摘 2026-09-10「ビジュアル的にもステージ 1 と大きく変わらず、敵キャラもオブジェクトも半分以上が graveyard の使いまわしで目新しいギミックもない」）。計測: ①敵は 7 種すべて第一章と同じ（第二章専用 0 種。`parseLevel` のスポーン: 第一章 zombie 6/angel 4/eye 2/bear 2/mushroom 2/unicorn 1/cake 1、第二章 mushroom 5/cake 4/angel 4/eye 3/bear 3/unicorn 1/zombie 1）②装飾は 8 個中 7 個が第一章と同じ（`DECO_MAP.candyforest` は graveyard の tomb/cross/flowers/candle/tree/blood/bones ＋ lollipop のみ）③ギミックは第一章と同じ「毒沼だけ」（動く足場・崩れる板・はしご・水流・風・ベルト・プレス・観覧車は 0。`~` が 68 → 116 タイルに増えただけ）④地形の起伏がほぼ同型（段差の変化 35 箇所で同数、足場の切れ目 20 対 21、地表行の集合は 1 行違い）。背景と地形帯は専用（各 6 層＋1 帯）だが、平坦な地面＋月＋同じ高さに並ぶ小物という画面構成が同じ。証跡 `test-results/shots/cmp_stage1_vs_2.png`。**要る判断: 生成予算**（残 14。専用敵 2 種 4・ギミック小物 2・装飾 3・中景差し替え 1 で計 8〜10 リクエストの見積り） | P1 |
| IMP-021 | 第二章の毒沼が平らな緑の板に見える（`THEMES.candyforest.bog` = #2f6a20/#3f8a28/#5cb040、glow #7cff70。ピンクの地面帯と明度差が大きい）。章名が「毒沼の菓子の森」なので毒の緑は残す前提で、境界に泡・糖蜜の縁取りを足すか、地面帯側の色を寄せる。**あわせて**: すり抜け足場 `=` が全テーマ共通の石の絵で、菓子の森では浮いて見える（`test-results/shots/cmp_stage2_12.png`） | 済（2026-09-10、0 リクエスト）。原因は 3 つ: ①沼の上の空気の列に背景の地平色（桃色一色）が素通しで、穴が板に見えた（穴の空気域 flat 0.59・11 色）②沼が 1 px の砂目ノイズで 3 倍表示では一様（明度 0.48、地面 0.32）③`=` は各テーマの地形帯から切っているが、菓子の森の帯 0.32〜0.42 が暗いチョコ礫で石に見えた。対応（`bogStyle: 'syrup'` を持つ菓子の森だけ。他 7 テーマの沼・足場・地表タイルの画素ハッシュは変更前後で一致 `tools/hash_tiles.mjs`）: 穴の奥壁（両岸の低い方の面から水面まで地中タイルを 55% 暗く）、糖蜜の水面（3 px の揺らぎ・輪の泡・桃色の砂糖粒・2 px の粒）、深部は地中帯の苔色へ、岸の滴り、`=` の帯を桃色の砂糖衣 0.20〜0.33 に。計測（`cmp_stage2_36_before.png` → `cmp_stage2_36.png`）: 穴の空気域 flat3x 0.59 → **0.13**・11 → 50 色、沼の明度 0.48 → **0.35**（地面 0.32）、沼の色数 5 → 11。`cmp_stage2_12.png`（`=`）、`cmp_stage2_60.png` |
| IMP-022 | **挿絵級の画風統一**（ユーザー指示 2026-09-10「画風は `assets/sprites/ending/scene1`・`scene6` が尊重物。それ以外の画像は不統一の修正対象」）。計測（`tools/style_check.py`、art-standard §1.3）: 尊重物は 32 色・flat 0.16/0.20・輪郭 #220d30/#1e0a24・主人公 4.3 頭身。対して cutin 4 枚は 24 色・flat 0.35〜0.40・輪郭 val 0.24〜0.30・**2.5 頭身**（3 軸すべて外）、scene3/4/5 は flat 0.30〜0.38・輪郭 val 0.29〜0.35、scene2 は輪郭 val 0.23 のみ外。原因: スプライト級の契約 `_style.txt`（チビ 2.5 頭身）とスプライト `idle_gold.png` を参照に渡していたので、絵師がスプライト側に寄った。**方法**: ①尊重物を基準文書に登録（art-standard §1.3・§2.6）②scene1 からキャラ立ち絵ベースを切り出し（`assets/gen/ref/lyrica-illust.png`、金衣装は衣装帯のみ色相回転で派生。0 リクエスト）③挿絵級の契約 `_illust_style.txt` と specs の `style` / `style_refs` で**毎回 尊重物＋立ち絵ベースを添付**（`gemini_gen.py`）④`style_check.py` が manifest に計測値を書き、`test/art-standard.test.js` §2.6 が帯を検査（既存の外れ絵は PENDING 列挙）。**対象と順**: (a) **済**（2026-09-10、台帳 226、1 リクエスト）: cutin 4 枚を尊重物 scene1＋立ち絵ベース（金）添付の 2×2 シートで再生成。計測: 32 色・flat **0.17〜0.23**（v1 0.35〜0.40、尊重物 0.16/0.20）・輪郭 val 0.19〜0.23・腰上構図で頭がパネル高の 1/4（v1 はチビ 2.5 頭身）。題名は下端に焼き込まれたので `crop_bottom: 12`。`test-results/shots/cutin_v2_vs_v1.png`、`super_*.png`（実機）。後処理 2 件を修正: 格子の隙間判定を「不透明 5% 未満」に（モデルが隙間の緑を #71a85f に寄せて描くとキーから漏れる）、`trim_border` に行の色数条件（粒子の空を額縁と誤認して heart の上 24 行が落ちた） (b) **済**（2026-09-10、台帳 227〜232 の 6 リクエスト）: ending scene3/4/5 を scene1＋立ち絵ベース（桃）添付で再生成。flat 0.30〜0.38 → **0.16 / 0.16 / 0.20**、輪郭 val 0.16〜0.23。scene3 は 2 回縦長（704×1472）に出たので 3 回目は参照を正方形切り抜き `lyrica-illust-sq.png` に変えて 262×258。scene5 は夜明けで最暗色 val 0.28 → `outline_val_max: 0.23` で 1 色だけ暗くした。`test-results/shots/ending_v2_vs_v1.png`、`ending_scene1..6.png`（黒帯 0） PENDING は空 (c) scene2 は帯内（輪郭 hue 311 / val 0.23）なので対象外 (d) タイトル画面・カタログ内の一枚絵は現状 生成物なし。**未決**: ゲーム内スプライト（チビ 2.5〜3 頭身、§2.3）を対象に含めるか。含めると約 200 リクエストの再生成で予算外のため、スプライト級は保留（ユーザー判断 2026-09-10）。挿絵級は済 | 済 |
| IMP-023 | **テスター計測の道具**（M5 出口条件「5 人・完走率 60%・平均 3 時間以内」を測るため。ユーザー指示 2026-09-10「resume them」） | 済（Sprint O）: `src/runlog.js`（run = 開始 1 回。開始章・到達章・クリア・死亡・コンティニュー・プレイ秒、`lyrica_runs` 50 件）、オプション「テスター報告を コピー」（`Game.copyReport` → JSON をクリップボードへ。版・UA・言語・画面・パッド名・run 集計・死亡集計。個人情報なし）、`docs/release/tester-guide.md`、`tools/tester_stats.mjs`（`docs/plan/logs/testers/*.json` → 完走率・平均分・章別到達）。`test/runlog.test.js` 3 件、`e2e/gamepad.spec.js` でコピーまで確認。**人の計測そのものは未実施** |
| IMP-024 | **実機ゲームパッドの確認手段** | 済（Sprint O）: オプションに「ゲームパッド」行（接続中のパッド名と押下中のボタン名を表示。`Input.padId`）。`e2e/gamepad.spec.js` が偽装パッド（standard mapping）で開始・ジャンプ・十字キー／スティック移動・診断行・割り当て変更を通す。**実機での確認は人待ち**（手引き `tester-guide.md` 4） |
| IMP-025 | **不具合報告の経路** | 一部済（Sprint O）: `.github/ISSUE_TEMPLATE/bug_report.yml`（日英、テスター報告 JSON 欄）・`config.yml`、README「不具合の報告・テスター計測」。済（2026-09-10）: 公開リポジトリ https://github.com/karak/ai-game-hackathon-sample（ユーザー指示で名前は ai-game-hackathon-sample）。Issues: https://github.com/karak/ai-game-hackathon-sample/issues/new/choose。README・known-issues・itch-page・HANDOFF に URL を記載 |
| IMP-026 | 人魚のスプライトを縦に長くする（胸〜腰まで描き、攻撃範囲＝当たり判定は維持したまま絵だけ調整） | P3 | ユーザー提案（2026-09-11）。現状は BUG-019 のクリップ修正で水面下に隠しているだけ。生成 1〜2 リクエスト（`mermaid1/2`、`specs.json` の box を縦に拡張し `part_sizes` で胴の長さを指定）。予算が要るのでユーザー判断 **済（2026-09-12、1 リクエスト、台帳 233/280）**: `enemy-mermaid` v2 を v1 raw 参照で生成（`part_sizes.mermaid`: 頭 40・髪幅 40・胴 20・腰 10・尾 36）。待機コマ mermaid1 42×53 → **62×66**（髪幅 40 → 40、色数 15 → 15、頭〜腰〜尾まで一体）、跳躍コマ mermaid2 78×77 → 77×81（15 色）。当たり判定は v1 の fitSprite 値 **8×15 に固定**（`MERMAID_W/H`）、水面上の頭の位置も不変（`MERMAID_HEAD_ABOVE` = 8 セルで上寄せ）、跳躍の発生距離 56・水面 `restY = waterY − 7` は同じ。水面下の体は α0.3 で水を透かして描く（`drawMermaidDoll`）。golden の gallery f0 ハッシュだけ更新（軌跡・他面は不変）。証跡 `test-results/shots/mermaid_v2_wait.png`・`mermaid_v2_lunge.png`（拡大 `*_zoom.png`、v1 比較 `mermaid_v1_wait_zoom.png`） |
| IMP-028 | 鏡像リリカは倒しても一定時間（20 秒ちょっと）で復活する（ユーザー指示 2026-09-13） | 済（2026-09-13） | `MIRROR_RESPAWN_T = 22`（`balance.js`）。倒したときの演出・得点は通常どおり出し、`gone` 秒の不在（姿も当たりも無し、軸の線だけ）の後に同じ軸へ hp 4 で復活。復活 1.5 秒前から薄く現れる。`test/mirror.test.js` 2 件。デモボットは不在中の鏡像を待たない |
| IMP-027 | 観測性（Sprint R）: 構造化ログ `LogEvent`（v/ts/sid/seq/rid/src/lvl/code/ctx/attr/err/build）、登録制コード `src/shared/logcodes.js`、`src/platform/log.js`（リングバッファ・console・sendBeacon）、Worker `worker/index.js` の `/api/log` → Workers Logs / `wrangler tail`、`tools/tail_logs.mjs`・`tools/log_stats.mjs`、ボットの `src:'bot'`、テスター報告 v2 | **済（2026-09-12、Sprint R、0 リクエスト）** | 計画 `docs/plan/09-observability.md`（作業 R1〜R7、DoD、リスク、§6 に計画との差分）。OTel・外部 SaaS は導入しないが §2 の写像で後から変換できる形。実装: 核 `src/shared/log.js`＋コード表 `src/shared/logcodes.js`（26 コード、未登録は throw・未使用も落とす `test/logcodes.test.js`）、`src/platform/telemetry.js`（console `[LYR code sid8]`、fetch keepalive で `/api/log`、15 秒／50 件／hidden／error、`window.__log`）、計装 14 箇所（main / Game / Player.hit / bossflow / input / audio / settings / runlog / deathlog / loader）、Worker `worker/index.js`（204/405/413/400、1 イベント 1 行＋`EDGE.LOG_BATCH`）、`wrangler.jsonc` `run_worker_first: ["/api/*"]`、`tools/tail_logs.mjs`・`tools/log_stats.mjs`、ボット 3 本の `setSource('bot')`、テスター報告 v2（`sid`＋直近 200 件）、オプション「ログそうしん」。テスト: Vitest 129 → 148、Playwright 11 → 13（`e2e/telemetry.spec.js`）。公開 URL で `check_dist` 282 読込・エラー 0、`wrangler tail` の NDJSON `docs/plan/logs/tail/2026-09-12-sprint-r.ndjson`（14 イベント、Worker 2 リクエスト 204）。**残**: 人の実操作 1 セッションのユーザー受入（DevTools で `[LYR` フィルタ → 報告 JSON コピー） |
| DEBT-011 | **コード構成を境界づけられたコンテキストで整理**（ユーザー指示 2026-09-11「DDD の概念でコンテクストを切り、コンポーネントを整理。回帰テストの保護を入れつつリファクタリング」） | 済（Sprint P）: `src/app`（進行）／`stage`（面）／`content`（作品データ）／`gfx`／`ui`／`platform`／`shared` に移設（`tools/move_contexts.mjs`、git mv で履歴維持）。切り出し: `stage/render.js`・`stage/collision.js`・`stage/viewport.js`、`app/screens.js`・`app/options.js`、`platform/keymap.js`（入力→設定の依存を反転）。game.js 422 → 229 行。護り: `e2e/golden.spec.js`（全 8 面の軌跡＋画素ハッシュ、リファクタリング前後で一致）、`test/architecture.test.js`（依存の向き 4 件）。後半（同日）: `entities/*.draw` 32 個を `stage/entityRender.js` へ（`drawEntity` が継承をたどって解決。`HD_SCALE` の正は `stage/viewport.js`）、ボス進行を `stage/bossflow.js` へ（world.js 177 行）。護りに「画廊」（全種を 1 画面に描く）を追加。architecture テスト 5 件、Vitest 127・Playwright 11 で画素ハッシュ一致 |
| BUG-016 | 私服・フルブルームの一部のコマにドレスの絵が混ざる（デプロイ版でユーザーが発見）。`assets.js` が manifest の**読み込み完了順**でコマを割り当てていたため、共通コマ（`run2` など）が衣装別コマ（`run2_plain`）を上書きすることがあった（`loadManifest` は `Promise.all` なので順序は不定） | 私服で歩く。再読込ごとに出るコマが変わる | 済 | 2 パスに変更（共通 → 衣装別で上書き）。`assignPlayerFrames` を切り出し `test/costume-frames.test.js` が順序をシャッフルして検査。あわせて衣装の色写像を全コマから作るようにし、コマ固有のピンク（cast1 の #b85170 など）も金・紺へ置換（衣装部分の桃色比 69% → 13〜21%） |
| BUG-017 | 棒付き飴の装飾が一度も画面に出ていなかった（`DECO_MAP.candyforest` の記号 `k` が敵マーカー「腐ったケーキ」と衝突し、`parseLevel` が敵として消費していた） | 第二章 | 済 | 記号を `l` に変更（Sprint L） |
| BUG-013 | 遊園地の装飾 `carousel` の左上に隣の `balloons` の目玉が混入（4 体並びの分割境界） | 済（`postprocess.py split_frames_masked`: x 範囲切りをやめ、コマに属する連結成分だけを残す。幅 8 未満の欠片は中心 x のコマへ帰属、期待コマ数指定時は最大コマの 3% 未満の欠片を最寄りへ。carousel 79×126→79×102、目玉分の高さが消えた） |
| BUG-009 | 帽子の二重描き（原画の帽子＋実行時の別パーツ）で帽子が 7 単位浮いていた。走りコマ 3 が 1 と同じ足、キー連打で位相リセット | 済（commit 予定: 重ね描き廃止、合成位置を base_hat 実測に、run-b 再生成、runT 保持。ユーザー指摘） |
| BUG-018 | 涙の川の 6 タイル沼（x=1216、tx 76-81）が縁からどんな入力でも渡れず、デモも人も手前で止まる | — | 済（2026-09-11、Sprint Q） | 本物の物理で総当たり（単発／二段 0〜48f／助走 0〜64px／トランポリン）した `tools/check_gaps.mjs` で「ステージ側」と特定（最大到達 x=1300、必要 >1300）。沼を 5 タイルに（`index.js` 涙の川 段 A 行 10-12）。第二章 x=2240 の段（W→足場→8 タイル沼）も限界超えだったので沼 6→5・踏み石の W を col 21→18 に。全横スクロール面で渡れない縁 0 を確認（ボス部屋は除外）。golden 更新。追記: 同区画の繭（Y）が隙間の真上（col 7）に吊られていて、跳んで下を通ると必ず落下と重なり被弾する配置だったので、着地側の足場の上（col 11）へ移動。デモの第二章は 2 死 → 0 死・3004 |
| BUG-022 | 鏡像リリカ 3 体（鏡の塔、各階 1 体）が同じ軸 x=136 を共有し、主人公が軸へ近づくと全員が主人公の階へ集まって 1 か所に重なっていた（y を主人公の y にしていた。撃つべき hp が 12 になり、デモボットも開始地点で 60 秒止まった） | 塔 | 済（2026-09-13） | 各鏡像は自分の階（`homeY` ±48）に主人公が居るときだけ鏡になり、それ以外は自分の階の軸で待つ（接触なし）。`test/mirror.test.js`。golden は塔と画廊のみ更新 |
| BUG-023 | 宝箱から出たアイテム（ドレス・金ドレス・武器・ポーション・1UP）が公開版でも旧文字列ドット絵（16×12 等、1 世界単位 = 3 px）で描かれていた。`assets.pickups` を生成素材の合流前に文字列シートから作っていたため（`pickups.dress.hd === false`） | 済（2026-09-13、Sprint S） | 合流後に `pickups` を作る（`src/gfx/assets.js buildAssets`）。golden は画廊（アイテム 9 個）だけ更新、8 面の軌跡・ハッシュ・メニューは不変。証跡 `test-results/shots/pickups_before_debt003.png` / `pickups_after_debt003.png` |
| BUG-024 | 二段ジャンプのほうき（`player.broomT` 0.45 秒）が一度も描かれていなかった。旧 `assets.broom` に `w`/`h` が無く描画座標が NaN | 済（2026-09-13、2 リクエスト、台帳 237〜238/280） | 文字列の 32×6 は 3 倍表示で HD 素材と縮尺が合わないため描かず、いったん `assets.broom = null`。ユーザー承認で HD 生成: `player-broom` v2（2 本横並び）→ `props/broom`・`broom2` **100×38 / 100×37 セル、15 色、fits true**（柄の同一 84 列から 59 列を `shorten_to` で削除。v1 単体は 171×36 で不採用）。主人公 idle 幅 69 の 1.45 倍（旧 32 世界単位 ≈ 1.4 倍と同等）。二段ジャンプ 0.45 秒に 12 Hz 2 コマ。golden は 8 面の 90 フレーム目と工房・塔の 600 フレーム目を更新（軌跡・メニュー・画廊は不変）。証跡 `broom_vs_idle_3x.png`・`broom_ingame_doublejump.png` |
| BUG-021 | 腐ったケーキ（RottenCake）が主人公の x へ寄って足場の縁から落ち、落下しながら蛆を吐き続ける。鏡の塔（縦面）では上の足場のケーキが次々に落ちてきて、下の主人公に蛆が降り注いだ（デモ収録ボットの死因追跡で検出。塔 x=59、落下中のケーキ 3 体が同じ列に居た） | 塔 | 済（2026-09-12） | 進む先の足元が床（solid / 片道床）でなければ歩かない（`enemies.js`）。golden は塔の軌跡・ハッシュだけ更新（敵数と死亡の有無が変わる。他 7 面・画面・画廊は不変） |
| BUG-020 | 針の群れ（NeedleSwarm）が突進の終わりに y を homeY+40 へ瞬間移動し、主人公の真上に現れて接触死になる（デモ収録ボットの死因追跡で検出。工房 x=1965、突進終了 0.55 秒で y 154 → 104 に跳んだ） | 工房 | 済（2026-09-12） | 瞬間移動を外し、高さは drift（30 px/s）で戻す（`enemies.js`）。golden の工房ハッシュは不変（90・600 フレームに突進終了が無い） |
| BUG-019 | 人魚のスプライト下端が切れて見え、水面に届いていない | — | 済（2026-09-11、Sprint Q） | 原因は描画のクリップ位置: 人魚は `waterY` で切っていたが、水の絵（`drawBog`）は水面タイルの 6 px 下から描くため、切れた縁が水面の 6 px 上に浮いていた。クリップを `waterY + 6` に（`entityRender.js drawMermaidDoll`、当たり判定・攻撃範囲は不変）。証跡 `test-results/shots/cmp_stage4_26.png` |
