# 既知の問題（v1.0 リリース候補、2026-09-09）

配信ページに載せる「既知の問題」の正。ID は `docs/plan/08-backlog.md` と同じ。修正したら両方から消す。

## プレイに影響するもの

| ID | 内容 | 回避 |
|----|------|------|
| IMP-017 | 難易度はボット（無敵なし・残機無限）の死亡ログで見ているだけで、人のテスト（5 人・完走率）は未実施（計測の道具は用意済み: オプション「テスター報告を コピー」→ `docs/release/tester-guide.md` → `tools/tester_stats.mjs`）。第三章の棘の谷（x512）、涙の川の沼（x1280）、工房のプレス機（x1088）、遊園地の沼（x2688）、菓子の森の沼（x2304）は死にやすい | コンティニューは無制限（面の先頭・スコア 0）。2 周目は 1 周クリアで開放 |
| — | 実機ゲームパッドでの検証は未実施（Gamepad API standard mapping。偽装パッドの E2E `e2e/gamepad.spec.js` で開始・ジャンプ・移動・割り当て変更の経路は確認済み）。実機スマホはユーザーが 2026-09-10 に確認 | オプション → ゲームパッド の行にパッド名と押下ボタン名が出る。出ない／違うときはその表示を報告し、「ジャンプ」「まほう」の行で再割り当て |
| IMP-013 | 第六章の強制スクロール区間は「走る車に乗る」代替。カメラ固定速度の強制スクロールではない | — |
| IMP-015 | タイトル放置時のデモは第一〜四章のみ。デモ中の操作はボット記録で、60 秒中に 2〜3 回死ぬ | — |
| — | ステージセレクトなし。「つづきから」は到達した最大の章から | — |

## 見た目

| ID | 内容 |
|----|------|
| BUG-012 残 | 被弾コマ（hurt）の帽子つばが待機コマの 1.45 倍（傾いた帽子）、落下（帽子なし）の髪幅 1.39 倍 |
| IMP-016 残 | 走り撃ちは 2 コマ（接地コマのみ）。通過コマは杖が描けず不採用 |
| IMP-018 | 2 周目「真の結末」の挿絵は場面 5 を流用 |
| BUG-006 | 私服の袖口・襟の色分けが単純（規則置換） |
| BUG-008 | 第三章（城）の中景が 1 種のみ |
| — | 空だけ 3 px/セル（城は 2）。遠景・中景は 1 px/セル |
| DEBT-003 | 炎とフォールバック用の旧文字列ドット絵が残っている（生成素材が読めないときだけ表示） |

## 素材について

- キャラクター・敵・ボス・背景・地形・アイテム・エンディング挿絵は Gemini 2.5 Flash Image で生成し、`tools/` で後処理（クロマキー→セル抽出→15 色量子化→帽子合成・衣装置換）したもの。原画は `assets/gen/raw/` に全て保存、プロンプトは `assets/gen/prompts/`、リクエストの台帳は `tools/gen_ledger.json`（186 件）
- 音楽・効果音は Web Audio API でリアルタイム合成。外部音源なし
- フォント DotGothic16（SIL OFL 1.1）

## 動作環境

- 確認済み: Chromium（Playwright headless）、macOS。初回ロード（フォント＋素材 235 枚の事前デコード）は localhost で 0.1〜0.3 秒、公開 URL（https://magical-lyrica.karak97.workers.dev）でエッジ未キャッシュ時 6.6 秒・キャッシュ後 0.6 秒（`window.__game.bootMs`、IMP-019）
- 60 fps 固定ステップ。処理落ち時は 1 フレームに最大 5 ステップまで進める
- 描画コスト実測: update 0.03 ms、draw 0.16 ms 以下／フレーム（`docs/plan/05-systems.md` 5.6）

## バグ報告

GitHub Issues（リポジトリ公開後）。公開先は https://magical-lyrica.karak97.workers.dev（`deploy.md`）。報告には「章・座標（ポーズ画面の位置は未表示なので、おおよその場所）・ブラウザ・再現手順」と、可能なら `localStorage` の `lyrica_deaths`（死亡地点ログ）を添えてください。
